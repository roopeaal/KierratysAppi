# Executive summary

KierrätysAppi has a runnable, well-tested source foundation, not a production-ready release. The implemented core is: contextual camera permission → supported EAN/UPC scan or manual GTIN → Fastify POST API → defensive Open Food Facts normalization → effective-dated component-level Finnish Rinki/Palpa guidance with visible source, date, confidence, preparation, exceptions and safe unknown/ambiguous states. A deterministic local EU material-code flow requires explicit visual confirmation and captures no image.

The adversarial audit independently verified the repository and repaired locally executable high-risk issues in cache bounds, provider identity/inference/quota/licensing, API validation/logging/configuration, mobile error semantics/privacy/permissions/accessibility, rule effectivity, database integrity and CI supply-chain enforcement. The latest continuation updates the compatible Expo set, patches newly found fast-uri highs and removes obsolete security exceptions. Final `corepack pnpm validate` passes with 154 application tests plus 13 repository-policy regressions (167 total), coverage and builds. On September 7 the fresh Expo 57 client established physical-iPhone Metro connectivity and passed Home, manual entry and a known-product result retaining uncertainty and Rinki provenance. Its first lookup correctly rendered offline while Metro held a stale API LAN address; restarting Metro with the current `EXPO_PUBLIC_API_BASE_URL` fixed the lookup. Async VoiceOver result behavior still needs device retest.

The release decision is **NO-GO**. Expo Doctor is now 20/20 with no release-age bypass. A fresh 2026-09-04 iOS Debug artifact builds, passes app/11-framework signing and installs; its September 7 current-client Home/manual/known-product development smoke also passes. The post-patch raw dependency snapshot has zero high/critical findings, but the final live registry audit times out and fails closed. There is still no Android binary or iOS archive/TestFlight build; the physical camera/accessibility/performance matrix, deployed API/database/monitoring/recovery evidence, approved OFF/content/privacy/legal positions and complete store assets/forms/console validation remain incomplete.

The controlling evidence and exact owner procedures are `PRODUCTION_AUDIT.md`, `RELEASE_GO_NO_GO.md`, `OWNER_ACTIONS.md` and `TEST_EVIDENCE.md`. Signing was limited to the previously authorized local Personal-Team development installation. No deployment, paid build, store action, contract acceptance, push or public publication occurred.
